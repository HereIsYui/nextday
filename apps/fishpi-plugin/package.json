{
  "name": "@nextday/fishpi-plugin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nextday/game-rules": "0.0.0"
  },
  "devDependencies": {}
}
