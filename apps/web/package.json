{
  "name": "@nextday/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "predev": "npm run build:packages --prefix ../..",
    "dev": "node ../../scripts/clean-next-cache.mjs . && next dev -p ${WEB_PORT:-3000}",
    "build": "next build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nextday/game-client": "0.0.0",
    "@nextday/game-rules": "0.0.0",
    "@nextday/ui": "0.0.0",
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
